## 開発ルール

- `pix_sort/` 配下のツールに仕様変更・機能追加を行った場合は、`pix_sort/doc/SPEC.md` に変更内容を反映すること

## Git ワークフロー

1. 作業開始前に最新の `main` ブランチを pull し、そこから作業ブランチを切ること
1. コードを変更したら必ずcommitすること
1. 作業完了後は PR を作成し、ブラウザで PR ページを開いてユーザーに確認依頼を出すこと
